# 이 파일을 `.env.local` 로 복사해서 값을 채운다 (.env.local 은 git 추적 안 함).
#
# GA4 측정 ID. 비워두면 Google Analytics 스크립트가 아예 렌더되지 않는다.
# 이 저장소를 fork 하거나 로컬에서 돌릴 때는 비워두는 게 정상 — 원저자의
# GA 속성으로 데이터가 가지 않는다.
#
# 배포(GitHub Actions)에서는 저장소 secret `NEXT_PUBLIC_GA_ID` 로 주입된다.
NEXT_PUBLIC_GA_ID=
